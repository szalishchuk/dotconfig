type = internal/network
interface = wlp7s0
interval = 3.0

format-connected = <label-connected>
format-connected-underline =
format-connected-foreground = ${colors.right-stripe1-foreground}
format-connected-background = ${colors.right-stripe1-background}
label-connected = "    %essid%   "

format-disconnected =
format-disconnected-foreground = ${colors.right-stripe1-foreground}
format-disconnected-background = ${colors.stripe-urgent}